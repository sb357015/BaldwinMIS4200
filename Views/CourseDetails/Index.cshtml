@model IEnumerable<BaldwinMIS4200.Models.CourseDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.coursedetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.coursedetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.coursedetailID })
        </td>
    </tr>
}

</table>
